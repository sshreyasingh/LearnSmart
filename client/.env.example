# Backend API URL — set this in Vercel (or your deploy) project env vars
# In development (Vite proxy handles /api), leave this unset
# In production: VITE_API_URL=https://your-backend-domain.com
VITE_API_URL=
