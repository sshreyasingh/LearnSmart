# Backend API URL — set this in Vercel (or your deploy) project env vars
# The code automatically adds /api, so set it WITHOUT the /api suffix
# Production: VITE_API_URL=https://rini.codes
# Development: leave unset (uses Vite dev proxy to localhost:5000)
VITE_API_URL=
